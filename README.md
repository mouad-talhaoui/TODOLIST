- first launch docker compose via the command:
```
docker-compose up
```

- to enter to docker type the command:
```
winpty docker exec -it todolist-db-1 mysql -uroot -p
```

- then create the tables# TODOLIST
